// P0 placeholder — village search (P1), the satellite map (P2), and
// polygon drawing (P3) land on this route in later phases. This page's
// only job right now is to prove the authenticated shell renders correctly.
export default function NewFarmPage() {
  return (
    <div className="flex flex-1 items-center justify-center p-8 text-center text-muted-foreground">
      <p>Village search and farm drawing arrive in the next phase.</p>
    </div>
  );
}
